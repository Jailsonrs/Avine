library(shiny)
library(dplyr)
library(DBI)
library(RMySQL)
library(RODBC)
ui <- fluidPage(
pageWithSidebar(
sidebarPanel(
selectInput("per","selecione o periodo", choices = Periodo),
selectInput("resp","selecione o responsável  pelo o indicador", choices =Responsável.pelo.Indicador)
),
mainPanel(
)
)
)
server <- function(input, output, session) {
if(input$all == F){
output$oi <- reactive(subset(dadosre,Responsável.pelo.Indicador == input$resp & Periodo == input$per ))
}
output$oi <- (subset(dadosre,Responsável.pelo.Indicador == input$resp & Periodo == input$per ))
}
shinyApp(ui,server)
