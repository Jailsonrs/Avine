ui <- fluidPage(
+     selectInput("per",
+                 "selecione o periodo",
+                 choices = Periodo),
+     selectInput(
+         "resp",
+         "selecione o responsável  pelo o indicador",
+         choices =Responsável.pelo.Indicador,
+         selected = sample(Responsável.pelo.Indicador)),
+     checkboxInput("vaz","vazias",value = FALSE),
+     tableOutput("tab"),
+ )
server <- function(input, output, session) {
Error in tag("div", list(...)) : argumento ausente, sem padrão
> + ui <- fluidPage(
+     selectInput("per",
+                 "selecione o periodo",
+                 choices = Periodo),
+     selectInput(
+         "resp",
+         "selecione o responsável  pelo o indicador",
+         choices =Responsável.pelo.Indicador,
+         selected = sample(Responsável.pelo.Indicador)),
+     checkboxInput("vaz","vazias",value = FALSE),
+     tableOutput("tab"),
+ )
+   C-c C-c
> ui <- fluidPage(
+     selectInput("per",
+                 "selecione o periodo",
+                 choices = Periodo),
+     selectInput(
+         "resp",
+         "selecione o responsável  pelo o indicador",
+         choices =Responsável.pelo.Indicador,
+         selected = sample(Responsável.pelo.Indicador)),
+     checkboxInput("vaz","vazias",value = FALSE),
+     tableOutput("tab"),
+ )
Error in tag("div", list(...)) : argumento ausente, sem padrão
> 